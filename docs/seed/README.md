# Seeds
- `demo_store.json` — dados mínimos da loja para onboarding.
- `demo_feed.xml` — feed XML com 60 itens (padrão Google).
- `demo_feed.csv` — alternativa CSV com mesmas colunas.

Use no onboarding: crie a loja, suba o feed, rode o Primeiro Diagnóstico (top 50).
